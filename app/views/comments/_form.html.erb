<%= form_for([@picture, @picture.comments.build]) do |form| %>
  <p>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </p>

  <%= form.hidden_field :user_id, :value => session[:user_id] %>

  <p>
    <%= form.submit %>
  </p>
<% end %>